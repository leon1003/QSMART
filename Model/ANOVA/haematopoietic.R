mydata = read.table('./TrainingSet/AvgRank/haematopoietic.csv',head=T,sep=",")
model = lm(IC50 ~ factor(Cancer) + factor(Drug), data=mydata)
sse <- c(crossprod(model$residuals))
sink('./Model/ANOVA/haematopoietic.txt',append=TRUE)
print(summary(model))
print(sse)
sink()
